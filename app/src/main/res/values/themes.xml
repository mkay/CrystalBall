<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Bare activity theme; all real UI styling lives in the Compose theme.

      Parented to AppCompat rather than the framework's Theme.Material because the activity is an
      AppCompatActivity — see the appcompat dependency for why. Nothing here is drawn: Compose
      paints over the whole window.
    -->
    <style name="Theme.CrystalBall" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
